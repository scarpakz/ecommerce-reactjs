import { NavBar } from "../components/NavBar"
import { Header } from "../components/Header"
export const HomePage = () => {
    return (
        <>
            <NavBar />
            <Header />
        </>
    ) 
}